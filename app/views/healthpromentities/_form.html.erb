<%= form_with(model: healthpromentity, local: true) do |form| %>
  <% if healthpromentity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(healthpromentity.errors.count, "error") %> prohibited this healthpromentity from being saved:</h2>

      <ul>
      <% healthpromentity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :mail %>
    <%= form.text_field :mail %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
